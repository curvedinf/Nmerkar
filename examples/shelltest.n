"printf 'alpha\nbeta\ngamma\n'" shell v0! v1! v2! v0 trim "\n" split v3! v3 length "lines=%d" format print v3 0 get "first=%s" format print 0 atomic v4! "for i in 1 2 3 4 5; do echo line$i; sleep 0.05; done" shell_stream v5! _addr v6 _addr v7 while list "echo" append "exec-works" append execute "exec-status=%d" format print "echo out; echo err 1>&2; exit 7" shell v8! v9! v10! v10 "sh-status=%d" format print v9 trim "sh-err=%s" format print v8 trim "sh-out=%s" format print "exit 0" shell v11! v12! v13! v13 "sh-status=%d" format print ret v6: v4 atomic_get "count=%d" format print v5 dequeue v14! v14 ret v7: v14 trim "got: %s" format print v4 1 atomic_add 0 ret 
